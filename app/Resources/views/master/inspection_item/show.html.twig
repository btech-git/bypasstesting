{% extends 'master/layout.html.twig' %}

{% block title 'Show Inspection Item' %}

{% block actions %}
    {{ include('master/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ inspectionItem.name|default }}</td>
            </tr>
            <tr>
                <th>Is Active</th>
                <td>{{ inspectionItem.isActive|default ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
