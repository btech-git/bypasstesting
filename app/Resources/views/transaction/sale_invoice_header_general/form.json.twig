{
    "subTotal": {{ saleInvoiceHeader.grandTotalBeforeDownpayment|default|json_encode|raw }},
    "taxNominal": {{ saleInvoiceHeader.taxNominal|default|json_encode|raw }},
    "grandTotal": {{ saleInvoiceHeader.grandTotalAfterDownpayment|default|json_encode|raw }},
    "saleInvoiceDetailGenerals": {
        {%- for index, saleInvoiceDetailGeneral in saleInvoiceHeader.saleInvoiceDetailGenerals -%}
            "{{ index }}": {
                "itemName": {{ saleInvoiceDetailGeneral.itemName|default|json_encode|raw }},
                "quantity": {{ saleInvoiceDetailGeneral.quantity|default|json_encode|raw }},
                "unitPrice": {{ saleInvoiceDetailGeneral.unitPrice|default|json_encode|raw }},
                "discount": {{ saleInvoiceDetailGeneral.discount|default|json_encode|raw }},
                "total": {{ saleInvoiceDetailGeneral.total|default|json_encode|raw }}
            }
            {%- if not loop.last -%},{%- endif -%}
        {%- endfor -%}
    }
}