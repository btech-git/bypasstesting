{% extends 'base.html.twig' %}

{% block title 'Memo' %}

{% block body %}
    <div class="memo">
        <!--<div class="header">
            <h2 class="title">PT. Bypassindo Jaya Indah</h2>
        </div>
        <div class="header center">
            <h1 class="title">VOUCHER</h1>
        </div>
        <br />-->
        
        <div class="header">
            <div><h3>{{ journalVoucherHeader.codeNumber|default }}<br/>
            {{ journalVoucherHeader.transactionDate|default ? journalVoucherHeader.transactionDate|date('j F Y') }}</h3></div>
        </div>
        
        <div class="body">
            <div class="right">
                <h3>
                    {{ journalVoucherHeader.totalDebit|default|number_format(0) }}<br/><br/><br/>
                    {{ journalVoucherHeader.totalCredit|default|number_format(0) }}
                </h3>
            </div>
        </div>
       
        <div class="note">
            <div class="table" rows="3">
                <div class="cell">&nbsp;&nbsp;</div>
                <div class="cell"><h3>{{ journalVoucherHeader.note|default }}</h3></div>
                <div class="cell" style="position: absolute">&nbsp;&nbsp;</div>
                <div class="cell"><h3>{{ journalVoucherHeader.totalDebit|default|number_format(0, '.', '')|say|capitalize }} rupiah.</h3></div>
            </div>
        </div>
        
       <!--<div class="note">
            <div class="table" >
                <div class="cell">&nbsp;</div>
                <div class="cell" style="text-align: left"><h3>{{ journalVoucherHeader.note|default }}<br/><br/><br/><br/>
                    {{ journalVoucherHeader.totalDebit|default|number_format(0, '.', '')|say|capitalize }} rupiah.</h3></div>
            </div>
        </div>
        <br/> &nbsp;-->
        
        <!--<div class="note">
            <div class="table full">
                <div class="cell col">
                    <div class="table full">
                        <div class="row">
                            <div class="cell label">No.</div>
                            <div class="cell value">{{ journalVoucherHeader.codeNumber|default }}</div>
                            <div class="cell label">Tgl.</div>
                            <div class="cell value">{{ journalVoucherHeader.transactionDate|default ? journalVoucherHeader.transactionDate|date('j F Y') }}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>-->
        
        <br />
        
        <!--<table class="detail">
            <thead>
                <tr class="bg-info">
                <th style="width: 15%">Kode</th>
                <th style="width: 25%">Akun</th>
                <th>Memo</th>
                <th style="width: 15%">Debit</th>
                <th style="width: 15%">Credit</th>
                </tr>
            </thead>
            <tbody>
                {% for journalVoucherDetail in journalVoucherHeader.journalVoucherDetails %}
                    <tr>
                        <td class="left">{{ journalVoucherDetail.account.code|default }}</td>
                        <td class="left">{{ journalVoucherDetail.account.name|default }}</td>
                        <td class="left">{{ journalVoucherDetail.memo|default }}</td>
                        <td class="right">{{ journalVoucherDetail.debit|default|number_format(0) }}</td>
                        <td class="right">{{ journalVoucherDetail.credit|default|number_format(0) }}</td>
                    </tr>
                {% endfor %}
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3" class="label right">Total</td>
                    <td class="right">{{ journalVoucherHeader.totalDebit|default|number_format(0) }}</td>
                    <td class="right">{{ journalVoucherHeader.totalCredit|default|number_format(0) }}</td>
                </tr>
            </tfoot>
        </table>
        
        <br />
        
        <div class="note">
            <div class="table">
                <div class="cell label">Terbilang:</div>
                <div class="cell">&nbsp;&nbsp;</div>
                <div class="cell">{{ journalVoucherHeader.totalDebit|default|number_format(0, '.', '')|say|capitalize }} rupiah.</div>
            </div>
        </div>
        
        <div class="note">
            <div class="table">
                <div class="cell label">Catatan:</div>
                <div class="cell">&nbsp;&nbsp;</div>
                <div class="cell">{{ journalVoucherHeader.note|default }}</div>
            </div>
        </div>-->
        
        <br />
        
        <!--<div class="signature">
            <div class="table full">
                <div class="cell sig">
                    <div>Penerima,</div>
                </div>
                <div class="cell sig">
                    <div>Hormat Kami,</div>
                    <div class="blank"></div>
                    <div class="blank"></div>
                    <div></div>
                </div>
            </div>
        </div>-->
    </div>
{% endblock %}

{% block stylesheets_include %}
    <link rel="stylesheet" href="{{ asset('css/memo2.css') }}" />
{% endblock %}

{% block javascripts_include %}{% endblock %}

{% block stylesheets %}
    <style>
        .col:nth-child(1) { width: 50% }
        .col:nth-child(2) { width: 50% }
        
        .col:nth-child(1) .label { width: 35% }
        .col:nth-child(1) .value { width: 65% }
        .col:nth-child(2) .label { width: 35% }
        .col:nth-child(2) .value { width: 65% }
        
        .sig:nth-child(1) { width: 50% }
        .sig:nth-child(2) { width: 50% }
    </style>
{% endblock %}